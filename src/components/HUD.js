import * as PIXI from '../pixi';
import c from '../utils/constants';

export default class StarScapeLayer extends PIXI.Container {
	constructor() {
		super();

		// free hud space: 279 * 49
	}
	opUpdate(delta) {}
}
